module.exports = {
    plugins: {
        // include whatever plugins you want
        // but make sure you install these via yarn!

        // browserslist is in package.json
        autoprefixer: {}
    }
}